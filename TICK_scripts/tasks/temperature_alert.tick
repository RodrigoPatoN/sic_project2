stream
  |from()
    .database('weather')
    .retentionPolicy('autogen')
    .measurement('weatherMesasurement')
  |alert()
    .crit(lambda: "temperature" > 30 OR "temperature" < 5 )
    .message('TEMPERATURE ALERT')
    .email()
        .to('pnogueira@student.dei.uc.pt')
